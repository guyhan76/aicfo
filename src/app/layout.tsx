import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "AICFO - Financial Intelligence Infrastructure",
  description: "AI Chief Financial Officer for risk detection and financial intelligence",
  keywords: ["CFO", "financial analysis", "risk detection", "US GAAP", "financial intelligence"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
